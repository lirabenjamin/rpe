import React from 'react';
import ReactDOM from 'react-dom';
import OneRMCalculator from './OneRMCalculator.tsx'; // Assuming your component is in the same file, or adjust the path if needed.

ReactDOM.render(<OneRMCalculator />, document.getElementById('root'));
